import { StyleSheet, Text, View } from "react-native";
import React from "react";

const PhotoOptionsScreen = () => {
	return (
		<View>
			<Text>Photo Options Screen</Text>
		</View>
	);
};

export default PhotoOptionsScreen;

const styles = StyleSheet.create({});
