import { StyleSheet, Text, View } from "react-native";
import React from "react";

const RoverView = ({ latestPhotosQuery, manifestQuery }) => {
	return (
		<View>
			<Text>RoverView</Text>
		</View>
	);
};

export default RoverView;

const styles = StyleSheet.create({});
